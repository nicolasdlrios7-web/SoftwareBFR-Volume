# apps/admin

Not scaffolded yet — out of scope for Phase 0 per `docs/roadmap/PHASE_0_FOUNDATION.md`. Will own moderation and internal-operator tooling when built. Owned by `frontend-engineer` + `backend-engineer` jointly when it starts.
