# @volume/observability

Owned by `backend-engineer`. Empty stub in Phase 0.

Shared logging/tracing/metrics setup for `apps/api` and `apps/worker`. `apps/web` and browser-side code should not depend on this package.
