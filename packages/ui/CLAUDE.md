# @volume/ui

Owned by `frontend-engineer`. Empty stub in Phase 0.

Shared React components used by `apps/web` (and `apps/admin` once it exists). No Volume-domain logic here — this is presentational/interaction primitives (buttons, layout, form controls), not feed or studio-specific components.
