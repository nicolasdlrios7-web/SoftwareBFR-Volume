# @volume/api-contracts

Owned by `contracts-engineer`. Empty stub in Phase 0.

Shared zod request/response DTOs between `apps/web` and `apps/api`. A DTO defined here must not duplicate a domain entity shape 1:1 by convention — it's the wire contract, and can diverge intentionally (e.g. omitting internal fields).
