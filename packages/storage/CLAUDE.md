# @volume/storage

Owned by `backend-engineer`. Empty stub in Phase 0.

Wraps object storage (asset uploads, Volume thumbnails) behind an interface so no other package talks to an S3-compatible SDK directly.
